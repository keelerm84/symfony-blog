<form action="{{ path('KoiosBlogBundle_comment_create', { 'blog_id' : blog_id } ) }}" method="post" {{ form_enctype(form) }} class="blogger">
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form>
